import { theme as defaultTheme } from "@chakra-ui/core";

const theme = {
  //   ...defaultTheme,
  fonts: {
    heading: '"GP", sans-serif',
    body: '"GP", sans-serif',
    mono: "Menlo, monospace",
  },
  colors: {
    ...defaultTheme.colors,
    // brandPurple: "#673FB4",
  },
};

export default theme;
